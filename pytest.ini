[pytest]
# Ensures extract/, transform/, load/, and config.py are importable as
# top-level packages regardless of where pytest is invoked from -- this
# matters especially in CI (GitHub Actions), where the working directory
# and Python path setup differ from a local interactive shell.
pythonpath = .
